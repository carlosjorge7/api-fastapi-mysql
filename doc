# Creamos un etorno virtual y lo activamos
virtualenv env
source ./env/Scripts/activate

# Instalamos las dependencias
pip install fastapi uvicorn sqlalchemy pymysql

uvicorn main:app --host="127.0.0.1" --port="3000" --reload
